[project]
name = "lock-gp"
version = "0.1.0"
description = "GPyTorch implementation of LOCK GP with accompanying demo on CR6261-H1 antibody dataset."
authors = [
    {name = "Martin Jankowiak", email = "jankowiak@gmail.com"},
    {name = "Yerdos Ordabayev", email = "yordabayev@generatebiomedicines.com"},
    {name = "Rudraksh Tuwani", email = "rtuwani@generatebiomedicines.com"},
]
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1.0",
    "gpytorch>=1.11",
    "botorch>=0.8",
    "pandas>=2.0",
    "numpy>=1.24",
    "scipy>=1.10",
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = ["pytest>=8", "ruff>=0.6"]

[tool.setuptools.packages.find]
include = ["lock_gp"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
extend-select = ["TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true

